apiVersion: v1
kind: Service
metadata:
  name: llm-api-service
spec:
  selector:
    app: llm-api
  ports:
  - port: {{ .Values.llmApi.port }}
    targetPort: {{ .Values.llmApi.port }}
    nodePort: {{ .Values.llmApi.nodePort }}
  type: NodePort
